clusterName:
provider:

global:
  service:
    annotations:

argocd:
  domain:

externalSecrets:
  serviceAccount:
    annotations:

externalDns:
  serviceAccount:
    annotations:

certManager:
  serviceAccount:
    annotations:
  dnsNames:
  issuer:
    email:
