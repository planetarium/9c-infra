{{- if .Values.yggGateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ygg-gateway
  namespace: {{ .Release.Namespace }}
  labels:
    app: ygg-gateway
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: ygg-gateway
spec:
  replicas: {{ .Values.yggGateway.deployment.replicas }}
  selector:
    matchLabels:
      app: ygg-gateway
  template:
    metadata:
      labels:
        app: ygg-gateway
    spec:
      {{- if .Values.imagePullSecret.secretKey }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecret.name }}
      {{- end }}
      containers:
        - name: ygg-gateway
          image: "{{ .Values.yggGateway.image.repository }}:{{ .Values.yggGateway.image.tag }}"
          imagePullPolicy: {{ .Values.yggGateway.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.yggGateway.deployment.port }}
          env:
            - name: PORT
              value: {{ .Values.yggGateway.deployment.port | quote }}
          envFrom:
            - secretRef:
                name: ragnarok-breaker-env
          livenessProbe:
            {{- toYaml .Values.yggGateway.deployment.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.yggGateway.deployment.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.yggGateway.deployment.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: Always
{{- end }}
