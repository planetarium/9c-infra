apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: arena-service
    app.kubernetes.io/instance: arena-service-test-20241218
  name: arena-service-deployment
  namespace: arena-service-test-20241218
spec:
  replicas: 1
  selector:
    matchLabels:
      app: arena-service
  template:
    metadata:
      labels:
        app: arena-service
    spec:
      containers:
        - name: arena-service
          image: planetariumhq/arena-service:git-eded06268a467d969dc90d7b2f2269874b9e02b5
          ports:
            - containerPort: 8080
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: arena-service-secret
                  key: POSTGRES_CONNECTION_STRING
            - name: Redis__Host
              value: redis-service
            - name: Headless__HeadlessEndpoint
              value: "https://odin-internal-rpc-1.nine-chronicles.com/graphql"