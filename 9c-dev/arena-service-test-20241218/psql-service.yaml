apiVersion: v1
kind: Service
metadata:
  labels:
    app: psql
    app.kubernetes.io/instance: arena-service-test-20241218
  name: psql-service
  namespace: arena-service-test-20241218
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: psql
  type: ClusterIP
